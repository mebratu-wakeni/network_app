# URL of the api backend when running the dev server against a separate host.
# Leave unset (or empty) in production -- the built app is served from the same
# origin as the api, under /admin, so relative URLs are used instead.
VITE_API_URL=
